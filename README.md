# ЮрПомощник Узбекистан

Справочный Telegram-бот по законодательству РУз с использованием OpenAI API. Языки: RU / UZ.

## Запуск локально

```bash
cp .env.example .env
# Заполните BOT_TOKEN, OPENAI_API_KEY
pip install -r requirements.txt
python -m app
```

## Docker

```bash
docker compose up -d
```

## Render

Рекомендуется **Web Service** с webhook (устраняет TelegramConflictError). Альтернатива: Background Worker. См. [RENDER.md](RENDER.md).

## Переменные окружения

| Переменная | Описание |
|------------|----------|
| BOT_TOKEN | Токен от @BotFather |
| OPENAI_API_KEY | Ключ OpenAI |
| OPENAI_MODEL | Модель (по умолчанию gpt-4o-mini) |
| TZ | Часовой пояс (Asia/Tashkent) |
| WEBHOOK_SECRET | Секрет для webhook (при деплое как Web Service) |

## Команды

- /start — выбор языка, приветствие, дисклеймер
- /help — помощь
- /lang — сменить язык
- /disclaimer — дисклеймер
- /privacy — конфиденциальность

## Меню

- ❓ Задать вопрос — ввод вопроса, ответ от OpenAI
- 🌐 Язык — выбор RU/UZ
- ℹ️ Дисклеймер — повтор дисклеймера
- 🆘 Срочный случай — чек-лист

## Важно

- Бот НЕ хранит вопросы и ответы
- Не отправляйте паспорт, адрес, ИНН, номера карт
- Информация справочная, не юридическая консультация
